SELECT AVG(closed_date::date - created_date :: date) as AVGage FROM github_issues where 'Epic' = ANY(label_arr) {{pod_select.selectedOptionValue ? " and '" + pod_select.selectedOptionValue + "' = ANY(label_arr)" : ""}} and closed_date >= '{{start_date_picker.formattedDate}}' and closed_date <= '{{end_date_picker.formattedDate}}' and state = 'closed'