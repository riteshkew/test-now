SELECT * FROM github_issues where 'Critical' = ANY(label_arr) {{pod_select.selectedOptionValue ? " and '" + pod_select.selectedOptionValue + "' = ANY(label_arr)" : ""}} and ((created_date >= '{{start_date_picker.formattedDate}}' and created_date <= '{{end_date_picker.formattedDate}}') or (closed_date >= '{{start_date_picker.formattedDate}}' and closed_date <= '{{end_date_picker.formattedDate}}'))